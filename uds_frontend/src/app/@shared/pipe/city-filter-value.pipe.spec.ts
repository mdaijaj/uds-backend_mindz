import { CityFilterValuePipe } from './city-filter-value.pipe';

describe('CityFilterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CityFilterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
