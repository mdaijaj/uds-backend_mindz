import { CategoryVideoSearchPipe } from './category-video-search.pipe';

describe('CategoryVideoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryVideoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
