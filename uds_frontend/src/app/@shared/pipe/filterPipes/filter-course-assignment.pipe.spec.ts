import { FilterCourseAssignmentPipe } from './filter-course-assignment.pipe';

describe('FilterCourseAssignmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCourseAssignmentPipe();
    expect(pipe).toBeTruthy();
  });
});
