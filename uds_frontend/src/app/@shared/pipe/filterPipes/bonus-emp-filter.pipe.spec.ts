import { BonusEmpFilterPipe } from './bonus-emp-filter.pipe';

describe('BonusEmpFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BonusEmpFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
