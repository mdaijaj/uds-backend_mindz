import { DepFilterPipe } from './dep-filter.pipe';

describe('DepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
