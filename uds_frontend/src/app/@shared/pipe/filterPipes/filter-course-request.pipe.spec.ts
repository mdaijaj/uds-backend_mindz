import { FilterCourseRequestPipe } from './filter-course-request.pipe';

describe('FilterCourseRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCourseRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
