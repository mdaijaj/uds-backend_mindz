import { RequestForFilterPipe } from './request-for-filter.pipe';

describe('RequestForFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestForFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
