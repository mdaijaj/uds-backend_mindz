import { CompoFilterPipe } from './compo-filter.pipe';

describe('CompoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
