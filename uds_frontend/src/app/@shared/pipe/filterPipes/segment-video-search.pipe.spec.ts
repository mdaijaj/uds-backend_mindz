import { SegmentVideoSearchPipe } from './segment-video-search.pipe';

describe('SegmentVideoSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SegmentVideoSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
