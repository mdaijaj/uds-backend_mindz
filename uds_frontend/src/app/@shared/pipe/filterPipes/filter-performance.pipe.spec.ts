import { FilterPerformancePipe } from './filter-performance.pipe';

describe('FilterPerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
