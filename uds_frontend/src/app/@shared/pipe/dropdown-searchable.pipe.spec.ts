import { DropdownSearchablePipe } from './dropdown-searchable.pipe';

describe('DropdownSearchablePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownSearchablePipe();
    expect(pipe).toBeTruthy();
  });
});
