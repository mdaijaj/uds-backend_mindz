import { AssignedCoursesPipe } from './assigned-courses.pipe';

describe('AssignedCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
