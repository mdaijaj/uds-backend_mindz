import { ListPrintPipe } from './list-print.pipe';

describe('ListPrintPipe', () => {
  it('create an instance', () => {
    const pipe = new ListPrintPipe();
    expect(pipe).toBeTruthy();
  });
});
