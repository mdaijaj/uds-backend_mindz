import { ItTicketingPipe } from './it-ticketing.pipe';

describe('ItTicketingPipe', () => {
  it('create an instance', () => {
    const pipe = new ItTicketingPipe();
    expect(pipe).toBeTruthy();
  });
});
